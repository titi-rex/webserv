	// std::string	pagePath = "." + v_host->getRoot();
	// std::string	reqUri = req.getUri();
	// std::string	reqPage = reqUri.substr(1, reqUri.size());

	// std::cout << "v_host->getRoot() = " << v_host->getRoot() << std::endl;
	// std::cout << "req.getUri() = " << req.getUri() << std::endl;

	// std::cout << "reqPage = " << reqPage << std::endl;

	// if(reqPage.substr(reqPage.find_last_of(".") + 1) == "html")
	// 	reqPage = reqPage.substr(0, reqPage.size() - 5);

	// std::cout << "reqPage = " << reqPage << std::endl;

	// if (v_host->locations.count("/" + reqPage) == 1)
	// 	pagePath += v_host->locations[reqPage].getRoot();
	// // v_host->locations[pagePath].root.empty();

	// if (reqUri != "/")
	// 	pagePath += reqUri;
	
	// pagePath += "/" + v_host->locations[reqUri].getIndex();

	// std::cout << "pagePath = " << pagePath << std::endl;
